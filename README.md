# GF2Recorder｜少前2后台任务助手

适用于《少女前线2：追放》官方 Windows 客户端的点击/滑动录制与后台任务工具。程序按进程 `GF2_Exilium.exe` 定位游戏，以 1920×1080 游戏客户区作为统一坐标，不通过 ADB，也不会在回放时移动真实鼠标。

## 使用

1. 将《少女前线2》PC客户端设置为窗口模式、1920×1080。
2. 启动游戏并进入主界面，再启动本程序。
   程序会自动请求管理员权限，以便读取游戏画面并录制游戏内输入。
3. 先点击“测试PC连接”；将游戏最小化后再测试一次，检查预览是不是当前游戏画面。
4. 按 Ctrl+Alt+F8 开始录制，在游戏窗口内完成一次流程，按 Ctrl+Alt+F9 停止。
5. 设置循环次数，点击“启动后台任务”；Ctrl+Alt+F10 可随时紧急停止。
6. 第一次迁移后建议开启调试模式逐步检查坐标，并用无消耗步骤确认后台点击生效。

## 后台运行与最小化

- 回放使用定向窗口消息，不抢占前台、不移动实体鼠标。
- 点击和滑动会发送到游戏实际输入窗口，并保留短暂按下时间，提高 DirectX 客户端兼容性。
- 程序优先尝试在游戏真正最小化时继续截图和操作。
- 如果游戏最小化后停止渲染，程序会尝试“屏幕外后台运行”：不激活游戏，只把窗口移出桌面并保持画面渲染；任务结束后恢复原窗口状态。
- DirectX游戏是否接受最小化截图和后台鼠标消息由客户端本身决定。首次使用请以无消耗步骤测试。

## 智能确认、状态校正与调试

- 点击步骤可设置智能确认：识别区域出现后再执行点击，也可按间隔重试前一个触发步骤。
- 点击步骤可设置双状态校正：目标状态直接确认，需校正状态先切换再确认。
- 调试模式每执行一步暂停并显示临时截图、点击位置、鼠标坐标和识别结果。
- 每一步支持修改等待时间、坐标、备注及删除。
- 支持录制并回放鼠标滚轮，步骤列表会显示滚动方向和格数。

## 旧数据迁移

新版数据保存在 `%LocalAppData%\GF2Recorder`。首次启动且新目录为空时，程序会从 `%LocalAppData%\LDRecorder` 复制任务、备注和识别模板，并保留旧目录作为备份。

## 安全提示

程序不会注入游戏、修改游戏数据或绕过保护机制。界面更新、网络延迟和游戏客户端对后台消息的支持情况都可能影响结果；自动操作也可能受到游戏服务条款限制，请自行评估并先用无消耗流程验证。

## 发布

安装 .NET 8 SDK 后，在项目目录执行：

```powershell
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
```

每次向 `main` 分支提交代码，GitHub Actions 都会在 Windows 环境自动还原依赖、编译并发布自包含单文件程序；成品直接发布到 Releases，不占用 Actions Artifact 配额，文件名为 `GF2Recorder-v版本号.exe`，同时附带 SHA-256 校验文件。也可以在 Actions 页面手动运行该流程。
